<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Gutenberg" parent="Theme.AppCompat.DayNight">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="titleTextColor">@color/text_title_color</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary_color</item>
        <item name="colorSecondaryVariant">@color/secondary_variant_color</item>
        <item name="colorOnSecondary">@color/text_title_color</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- added custom font -->
        <item name="fontFamily">@font/montserrat</item>
        <item name="android:titleTextAppearance">
            @style/TextAppearance.MaterialComponents.Headline1
        </item>
    </style>

    <style name="Theme.Gutenberg.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="titleTextColor">@color/text_title_color</item>
        <!-- added custom font -->
        <item name="fontFamily">@font/montserrat</item>
        <item name="android:titleTextAppearance">
            @style/TextAppearance.MaterialComponents.Headline1
        </item>
    </style>
</resources>